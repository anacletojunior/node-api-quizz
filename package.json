{
   "name": "quiz-api",
  "version": "1.0.0",
  "description": "API de Quiz com JSON Server",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "NODE_ENV=development node api/server.js",
    "dev-win": "set NODE_ENV=development && node api/server.js"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
